# Copy to .env and fill in. Only needed for live runs of `npm run find-bugs`.
# `npm run demo` and the test suites do not need credentials.
#
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides.
# ANTHROPIC_BASE_URL=https://api.anthropic.com
